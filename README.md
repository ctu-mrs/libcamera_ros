# libcamera catkin wrapper

This implements a thin catkin wrapper around the [libcamera](https://libcamera.org) meson project.
It only builds the main library without examples, tests or documentation.
