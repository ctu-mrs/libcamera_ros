name: generic_package_build

on:

  push:
    branches: [ ros2_devel ]

  pull_request:
    branches: [ ros2 ]

  workflow_dispatch:

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:

  build:
    uses: ctu-mrs/ci_scripts/.github/workflows/generic_package_build.yml@ros2
    secrets:
      PUSH_TOKEN: ${{ secrets.PUSH_TOKEN }}
